Name:           ros-hydro-pr2-navigation
Version:        0.1.22
Release:        0%{?dist}
Summary:        ROS pr2_navigation package

Group:          Development/Libraries
License:        BSD
URL:            http://ros.org/wiki/pr2_navigation
Source0:        %{name}-%{version}.tar.gz

BuildArch:      noarch

Requires:       ros-hydro-laser-tilt-controller-filter
Requires:       ros-hydro-pr2-move-base
Requires:       ros-hydro-pr2-navigation-config
Requires:       ros-hydro-pr2-navigation-global
Requires:       ros-hydro-pr2-navigation-local
Requires:       ros-hydro-pr2-navigation-perception
Requires:       ros-hydro-pr2-navigation-self-filter
Requires:       ros-hydro-pr2-navigation-slam
Requires:       ros-hydro-pr2-navigation-teleop
Requires:       ros-hydro-semantic-point-annotator
BuildRequires:  ros-hydro-catkin

%description
The pr2_navigation stack holds common configuration options for running the

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
mkdir -p build && cd build
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/hydro" \
        -DCMAKE_PREFIX_PATH="/opt/ros/hydro" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
cd build
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/hydro

%changelog
* Mon Sep 08 2014 Maintained by Eitan Marder-Eppstein <eitan@willowgarage.com> - 0.1.22-0
- Autogenerated by Bloom

